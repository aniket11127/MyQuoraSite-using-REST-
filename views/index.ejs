<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Quora Posts</title>
    <link rel="stylesheet" href="/style.css">
</head>

<body>
    <h1 class="title">Quora Posts</h1>

    <% for(let post of posts){ %>
        <div class="post">
            <h3 class="username">username : @<%= post.username %>
            </h3>
            <h4 class="content">content : <%= post.content %>
            </h4>
            <a href="http://localhost:8080/posts/<%= post.id %>">read more</a> &nbsp;
            <a href="http://localhost:8080/posts/<%= post.id %>/edit">edit post</a>
            <br><br>
            <form method="post" action="/posts/<%= post.id %>?_method=DELETE">
                <button>delete</button>
            </form>
        </div>
    <% } %>

    <br><br>
    <a href="http://localhost:8080/posts/new">add post</a>


</body>

</html>